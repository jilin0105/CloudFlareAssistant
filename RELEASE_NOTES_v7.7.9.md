### **v7.7.9 更新日志**

> **提示：** 本次更新无破坏性变更，可直接覆盖安装。

#### 新增：Zero Trust 网关 DNS 查询分析

- Gateway 模块新增「分析」标签页，提供完整的 DNS 查询分析功能
- 支持 5 个分析维度：DNS 操作、DNS 国家/地区、排名靠前的 DNS 位置、阻止次数最多的用户、允许次数最多的用户
- 支持 Top N 条目数切换（5 / 10 / 20 / 50 条），各区块按查询次数降序展示
- 基于 `gatewayResolverQueriesAdaptiveGroups` 数据集，复用现有 GraphQL 基础设施
- 支持时间范围筛选，解析决策（resolverDecision）数值自动映射为中文描述
- 阻止 / 允许用户维度按决策类型聚合，无身份信息时统一展示为「未捕获标识信息」

#### 新增：资源列表长按复制 ID

- 为 D1 数据库、KV 命名空间、R2 存储桶列表项添加长按复制 ID 功能
- 复制成功后弹出 Toast 提示，提升资源 ID 管理与排查效率

#### 优化：模板商店 Pages 部署绑定生效

- 模板商店 Pages 部署新增新建项目二次部署逻辑，确保 KV / D1 / R2 等绑定配置正确生效
- 修复首次部署后绑定未生效的问题

#### 优化：R2 列表项 UI 布局

- R2 存储桶列表项新增 Bucket ID 展示行，与 D1 / KV 列表布局风格保持一致

本次更新完成 Zero Trust 网关 DNS 查询分析、资源列表长按复制 ID、Pages 部署绑定修复与 R2 列表布局优化，建议所有使用 Zero Trust 与资源管理功能的用户升级。
